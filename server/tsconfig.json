{
    "compilerOptions": {
        "strict": true,
        "lib": ["ES2017", "ES2016", "ES2018", "ES2015", "ES2019", "ES2020"],
        "moduleResolution": "node", // 또는 Classic 
        "esModuleInterop": true, // export default 가 되지 않아도 자동으로 import 가 되지 않아도 바로 import 가능 (asterik 쓰지 않아도 됨)
        "typeRoots": ["./types"]
    }
}